name: Copy Container

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  copy:
    runs-on: macos-latest

    steps:
      - name: show
        run: system_profiler SPHardwareDataType
